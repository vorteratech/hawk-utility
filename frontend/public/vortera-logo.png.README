Drop the Vortera Technologies logo here as `vortera-logo.png`.

Recommended:
- Transparent background (PNG)
- Roughly 240x60 px (or larger; the layout scales it down)
- Keep this README out of the way once the real file is in.
